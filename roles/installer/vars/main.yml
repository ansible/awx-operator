---
postgres_initdb_args: '--auth-host=scram-sha-256'
postgres_host_auth_method: 'scram-sha-256'
ldap_cacert_ca_crt: ''
bundle_ca_crt: ''
projects_existing_claim: ''
path_to_pg_version: /var/lib/postgresql/data/pgdata/PG_VERSION # replace this with the PG_VERSION path inside the downstream image if different
